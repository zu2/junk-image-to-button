# junk-image-to-button
Hides images labeled with captions such as “Getty Images” and “AI-generated illustration”. Replaces them with a button so you can reveal them only when needed. Works with dynamically loaded content.
